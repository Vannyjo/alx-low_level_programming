#include "main.h"
/**
 * print_alphabet - print alphabet in lower case
 * Return: Always 0 (success)
 */
void print_alphabet(void)
{
	char eze;

	for (eze = 'a'; eze = 'z'; eze++)
	{
	_putchar(eze);
	}
	_putchar('\n');
	return (0);
}
