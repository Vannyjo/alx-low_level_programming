#include "main.h"
/**
 * _strlen_recursion - Entry point
 * @s: Returns the length of the string
 * Return: 0
 */
int _strlen_recursion(char *s)
{
	if (*s)
	{
		int  _strlen_recursion(char *s) + 1
		 _putchar(*s);
	}
	return ('\0');
}
